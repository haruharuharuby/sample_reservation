%footer
  .container
    - if logged_in?
      %ul
        %li= link_to "予約管理", home_path
        %li.split |
        %li= link_to "スタッフ業務", staff_path
        - if @current_user
          %li.split |
          %li= link_to "顧客情報", "/users/#{@current_user.id}"
          %li.split |
          %li= link_to "予約履歴", "/users/#{@current_user.id}/reshistory"
          %li.split |
          %li= link_to "物販購入履歴", "/users/#{@current_user.id}/mathistory"
